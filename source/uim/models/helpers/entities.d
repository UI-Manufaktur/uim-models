module uim.models.helpers.entities;

@safe:
import uim.models;

/* Json toJson(DEntity[] entities) {
  auto result = Json.emptyArray;

  entities.each!(e => result ~= e.toJson);

  return result;  
}
/// 
unittest {
  auto ent1 = new DEntity;
  auto ent2 = new DEntity;

  auto json = [ent1, ent2].toJson;
  assert(json.length == 2);
}

 */